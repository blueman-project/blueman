themedir = $(icondir)/hicolor
size = 24x24
context = actions

iconsdir = $(themedir)/$(size)/$(context)

icons_DATA =				\
	blueman-trust.png		\
	blueman-untrust.png		\
	blueman-send-file.png

EXTRA_DIST = $(icons_DATA)
